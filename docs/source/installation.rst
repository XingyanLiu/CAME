Installation
============

PyPI
~~~~

Install stagewiseNN with PyPI, run:

.. code-block:: python

   pip install came

GitHub
~~~~~~

Or fetch from GitHub and manually install:

.. code-block:: shell

   git clone XXX
   cd CAME
   python setup.py install

END